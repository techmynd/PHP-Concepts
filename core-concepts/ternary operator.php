<?php
  
  /*

  $score = 50;
  if($score > 40){
    echo "high score";
  } else {
    echo "low score";
  }

  */

  // ternary operator

  $score = 50;
  // $score > 40 ? 'high score' : 'low score';
  $result = $score > 40 ? 'high score' : 'low score';
  echo $result;
  // echo $score > 40 ? 'high score' : 'low score';

?>